spring:
  datasource:
    url: jdbc:mysql://localhost:3306/coreplm_db?createDatabaseIfNotExist=true
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:9597352352}

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.coreplm: DEBUG
    org.hibernate.SQL: DEBUG